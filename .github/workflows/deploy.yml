name: Deploy Theme to cPanel
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger deploy hook
        run: |
          curl -fsS --max-time 120 \
            "https://lp.premiaspine.com/?premia_deploy=${{ secrets.DEPLOY_SECRET }}"
